[TEMPLATE]
# Copy to .env. Required: the app calls Express directly, there is no Vite proxy.
# The dev backend runs at http://localhost:4000 (see PORT in Backend/.env).
VITE_API_URL=http://localhost:4000
